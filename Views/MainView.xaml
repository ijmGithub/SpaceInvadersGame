<UserControl x:Class="SpaceInvadersGame.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SpaceInvadersGame.ViewModels"
             xmlns:views="clr-namespace:SpaceInvadersGame.Views"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="820">
    <UserControl.DataContext>
        <vm:MainViewModel />
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Menu" Command="{Binding NavigateMenuCommand}" Width="100" Margin="0 0 8 0" />
            <Button Content="Juego" Command="{Binding NavigateGameCommand}" Width="100" Margin="0 0 8 0" />
            <Button Content="Puntuación" Command="{Binding NavigateScoreCommand}" Width="100" />
        </StackPanel>

        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vm:MenuViewModel}">
                    <views:MenuView />
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:GameViewModel}">
                    <views:GameView />
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:ScoreViewModel}">
                    <views:ScoreView />
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</UserControl>
